notify:
  - name: ga_broadcast
    platform: rest
    resource: !secret google_home_broadcast_url

google_assistant:
  project_id: !secret google_assistant_project_id
  exposed_domains:
    - switch
    - light
    - script
